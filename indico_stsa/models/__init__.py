"""Database models, all in the ``plugin_stsa`` schema."""

#: Every model in this package must live here -- Indico refuses to load a
#: plugin that adds a model outside its own ``plugin_*`` schema.
SCHEMA = 'plugin_stsa'
